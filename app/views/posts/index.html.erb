<h1>This is category <%= @category.name%> Posts</h1>

<h3>Here are a list of post: </h3>

<h3>Post Name</h3>


  <% @posts.each do |post|%>
  <ul>
    <li><%= link_to post.name, category_post_path(category_id: @category.id, id: post.id)%></li>
  </ul>
  <%end%>
  
<%= link_to "Create New post", new_category_post_path %><br>

<%= link_to "Back", category_path(@category)%><br>
